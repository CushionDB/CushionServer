{
  "name": "cushion-server-master",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "clean": "rm -rf build && mkdir build && mkdir build/util",
    "build-server": "babel ./server.js --out-dir ./build",
    "build-utils": "babel ./util/util.js --out-dir ./build/util",
    "build-app": "babel app.js --out-dir ./build",
    "build": "npm run clean && npm run build-utils && npm run build-server && npm run build-app",
    "dev": "npm run build && node ./build/app.js",
    "start": "node ./build/app.js",
    "test": "jest"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "btoa": "^1.2.1",
    "cors": "^2.8.5",
    "express": "^4.17.1",
    "node-fetch": "^2.6.0",
    "web-push": "^3.3.5"
  },
  "devDependencies": {
    "@babel/cli": "^7.1.0",
    "@babel/core": "^7.5.5",
    "@babel/preset-env": "^7.5.5",
    "jest": "^24.8.0"
  }
}
